# aigc-rewrite

> 学术文本 AIGC 降率改写 Skill,目标稳定将课题申报书/论文的 AIGC 检测率从 80%+ 降至 **25-35%** 区间。
> 适配 **维普 2026 多模型交叉验证**,兼容知网 AIGC、朱雀论文版。

## 项目简介

这是一个为 Claude Code 设计的本地 Skill,封装了完整的学术文本降 AIGC 率知识库与辅助工具:

- **改写指令**: 五大维度(句法/段落/密度/词汇/标点) + 学术专用第六维(批判性注入)
- **章节加权策略**: 摘要 ×1.8 / 引言 ×1.5 / 结论 ×1.5 等差异化处理
- **拼接预警规避**: 跨章节句长/人称/复杂度一致性检查
- **数据密集化**: 主动添加 β/p/R²/n 等精确统计反而降率
- **Python 辅助工具**: 指纹分析、长句拆分、AI 爱用词替换、章节切分等

**核心理念**: AI 洗稿 → 人工注血 → AI 润色 → 人工定稿,**必须人机配合**。维普 2026 可识别同一文本中的 AI 片段,所以【人工补充】部分必须真人手写。

## 适用场景

| 场景 | 触发词示例 |
|-----|----------|
| 课题申报书降重 | "帮我把这段降AIGC"、"这段维普查出来82%,降一下" |
| 论文章节改写 | "这是论文摘要,降到30%以内"、"文献综述AIGC率太高" |
| 多平台预检 | "想同时过维普和朱雀"、"知网AIGC怎么改" |
| 拼接预警排查 | "整篇论文检查跨章节一致性" |

## 文件结构

```
aigc-rewrite/
├── SKILL.md          # Skill 入口(含 frontmatter,Claude 自动加载)
├── reference.md      # 完整原理参考(五大维度/章节加权/批判注入)
├── checklist.md      # 人工注血核验清单(每篇必跑)
├── analyze.py        # 文本指纹分析工具(支持章节加权)
├── rewrite_helper.py # 改写辅助工具(7个子命令)
└── README.md         # 本文件
```

## 通过 Claude Code 使用

### 方式一: 自然语言触发

直接告诉 Claude 你的需求,描述中含触发词(AIGC降重/降AI率/维普AIGC/洗稿/拼接预警/朱雀AIGC 等)即可自动加载本 Skill:

```
"我有一段论文摘要,维普查出来 78%,帮我降到 30% 以内"
"@thesis.md 这是文献综述,做批判性注入"
"整篇申报书做拼接预警检查"
```

### 方式二: Skill 命令

```
/aigc-rewrite
```

### 必要输入

Claude 会要求你提供以下信息(可在首条消息一并给出):

1. **待降率文本**(直接粘贴或文件路径)
2. **章节类型**: 摘要 / 引言 / 文献综述 / 方法 / 结果 / 结论 / 致谢 / 局限 (决定改写幅度)
3. 目标 AIGC 率(默认 25-35%)
4. 目标检测平台(默认维普;若多平台,优先按朱雀标准改)
5. 是否有可用的本地数据/访谈/统计结果(用于人工注血提示)

## 通过 Codex / GitHub Copilot 使用

本 Skill 的核心价值在于**方法论与辅助工具**,不局限于 Claude Code。在 VS Code + Copilot、OpenAI Codex CLI、Cursor 等环境中,按以下方式复用:

### 方式一: 直接调用 Python 工具链

两个脚本无外部依赖,可在任何环境中独立运行。只需把 `analyze.py` 与 `rewrite_helper.py` 放入工作区,即可在 Copilot Chat 中直接请求:

```
"运行 analyze.py 检查当前文件"
"用 rewrite_helper.py cohesion 检查全文拼接预警"
```

### 方式二: 将方法论文件喂给 AI 上下文

把 `reference.md` 或 `checklist.md` 作为上下文附件(attachment)或粘贴到对话中,然后请求改写:

```
请按 reference.md 中的五大维度改写以下段落:
- 句法: 拆长句、长短交替、语态混用
- 段落: 制造思维跳跃(设问/回溯/岔开)
- 密度: 段落胖瘦不均
- 词汇: 替换AI爱用词、插入俗语、四字对仗打散
- 标点: 逗号:句号 2:1~2.5:1,增加破折号与括号

原文:
[粘贴文本]
```

### 方式三: 按章节类型给精确指令

参考 `SKILL.md` 中的章节加权表,在提示词中明确章节类型,让 Codex 按正确幅度改写:

| 你输入的提示 | Codex 应执行的改写幅度 |
|------------|---------------------|
| "这是**摘要**,深度改写 80%" | 打破四段式、加数据、加学科术语、结尾加"超出预期" |
| "这是**文献综述**,加批判性注入" | 不改陈述,每段引述后加 1 句方法/样本/时效批判 |
| "这是**研究结论**,深度改写" | 按引言顺序逐一回应、加"超出预期"发现 |
| "这是**背景/局限**,轻度改写" | 仅轻度改写,主要拉齐风格一致性 |

### 方式四: 使用【人工补充】模板

无论使用哪个 AI 工具,都让 AI 在输出中用 `【人工补充:提示】` 标记需要真人手写的内容。可给 Codex 的指令模板:

```
改写完成后,在以下位置插入【人工补充】标记(内容留空,由用户手写):
1. 数据位置: 【人工补充:不精确的本地数据,如"约X家但配套不足X%"】
2. 访谈位置: 【人工补充:1-2句匿名访谈原话】
3. 转折位置: 【人工补充:真实调研转折,如"一度倾向于...但..."】
4. 时间锚点: 【人工补充:具体月份或政策节点】
5. 批判位置: 【人工补充:对前句研究的方法/样本批判】
```

> **注意**: Codex/Copilot 可能会主动尝试填写【人工补充】内容。**必须删除 AI 填写的内容,改由用户亲笔重写**,否则维普 2026 可识别同一文本中的 AI 片段,导致降率失败。

## 直接使用 Python 脚本

两个脚本无外部依赖,仅用 Python 标准库,Windows / macOS / Linux 通用。

### analyze.py — 指纹分析

```powershell
# Windows PowerShell
$env:PYTHONIOENCODING="utf-8"
python "C:\Users\hubo0\.claude\skills\aigc-rewrite\analyze.py" <文件>

# 章节加权评分(摘要 ×1.8)
python analyze.py --chapter 摘要 <文件>

# JSON 输出
python analyze.py --json <文件>

# 改写前后对比
python analyze.py --diff <旧文件> <新文件>
```

```bash
# POSIX
python ~/.claude/skills/aigc-rewrite/analyze.py <文件>
```

输出报告含: 平均句长、逗号:句号比、第一人称密度、AI 爱用词、TTR、引用密度、统计数据密度、机械列举警告、拼接预警、风险评分(0-100)。

### rewrite_helper.py — 改写辅助

```powershell
python rewrite_helper.py <子命令> <文件>
```

| 子命令 | 用途 |
|-------|-----|
| `locate` | 定位需改写位置(长句/AI 爱用词/并列结构) |
| `suggest` | 生成【人工补充】候选标记位置 |
| `split` | 长句拆分建议(基于标点) |
| `replace` | AI 爱用词替换建议 |
| `chapters` | 识别文档章节并按权重排序 |
| `cohesion` | **跨章节拼接预警检测**(句长/人称/复杂度) |
| `critique` | **文献综述批判性注入候选位** |

## 工作流程

```
原文 (AIGC 率 80%+)
    │
    ▼
┌─────────────────────────────────────────────┐
│ 第一步: AI 洗稿 (Claude 完成)                │
│  - 六大维度破坏性改写                         │
│  - 标注【人工补充】点位 (≥5 处)              │
└─────────────────────────────────────────────┘
    │
    ▼  (AIGC 率 ~50%)
┌─────────────────────────────────────────────┐
│ 第二步: 人工注血 (用户必须亲笔)               │
│  - 模糊数据/精确数据/访谈引语                 │
│  - 自我怀疑/具体障碍/时间锚点                 │
│  - 引用支撑/批判注入/意外发现                 │
└─────────────────────────────────────────────┘
    │
    ▼  (AIGC 率 ~35%)
┌─────────────────────────────────────────────┐
│ 第三步: AI 润色 (Claude 完成)                │
│  - 只调语言,不改结构                         │
│  - 保持口语化痕迹,确保学术庄重                │
└─────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────┐
│ 第四步: 章节交叉检查 + 人工定稿                │
│  - 运行 cohesion 检查拼接预警                 │
│  - 通读保留 1-2 处不完美口语化                │
└─────────────────────────────────────────────┘
    │
    ▼
最终稿 (AIGC 率 25-35%) ✓
```

## 输出示例

**analyze.py 报告片段**:

```
【章节信息】
  章节类型:           摘要
  权重系数:           ×1.8
  策略:               高权重(必须深度改 80%)

【风险评分】
  AI 风险:            70/100  [██████████████░░░░░░]
  加权风险(×1.8):    100/100  [████████████████████]
  ❌ 高风险: 维普 AIGC 率预估 60%+,需深度改写

【风险点】
  ⚠️  平均句长 82.9 字过长 (>40),需拆句
  ❌ 机械列举 23 处,维普识别为 AI 模式(率可达 85%+)
  ⚠️  引用密度 0.0/千字过低 (目标 ≥3)
```

**cohesion 拼接预警检测**:

```
⚠️  句长差异 446 字 (>5字易触发拼接预警)
     最长: 「四、急救包」 461.5字 vs 最短: 「情绪/评价词」 15.5字

⚠️  人称混用: 我们=7 / 课题组=7 / 笔者=1
     建议全文统一用一种人称,避免拼接预警
```

## 核心原则

- **绝不替用户写【人工补充】内容**: 必须真人手写,保持人机分离。维普 2026 可识别同一文本中的 AI 片段。
- **章节加权差异化**: 摘要×1.8 / 引言×1.5 / 结论×1.5,改写幅度按权重分配。
- **关键章节单独达标**: 整体率不是唯一指标,关键章节风险等级必须为"低"。
- **拼接预警警觉**: 全文人称统一/句长一致(±5字)/用词复杂度均匀,低权重章节也要轻度改。
- **分阶段降率**: 先 AI 洗到 50%,人工注血到 35%,通读保留不完美到 30%。不要试图一次到位。
- **数据密集化反直觉**: 凡能写精确数据(β=0.47, p<0.01, R²=0.62)处必写,反而降率。
- **二次检测反升警告**: 不要用 ChatGPT/文心一言/Claude 二次改写已有 AI 内容,会强化 AI 特征。

## 实战案例

教育学硕士论文 12000 字:

| 阶段 | 整体率 | 摘要率 | 耗时 |
|-----|-------|--------|------|
| 初始(纯 AI 生成) | 58% | 76% | - |
| 第 1 轮(深度改摘要+引言) | 42% | 38% | 8 分钟 |
| 第 2 轮(标准改综述+设计) | 35% | 35% | 5 分钟 |
| 第 3 轮(人工微调) | **22%** | **28%** | 1 小时 |

**总耗时 1.5 小时**,相比纯人工 8-10 小时提升 5-6 倍。

## 详细文档

- [`SKILL.md`](./SKILL.md) — Skill 主指令
- [`reference.md`](./reference.md) — 完整原理参考
- [`checklist.md`](./checklist.md) — 人工注血核验清单

---

*本 Skill 基于维普 AIGC 检测 2026 年多模型交叉验证机制构建,建议配合实际检测迭代优化。*
